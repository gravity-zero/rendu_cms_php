launch project : ```docker-compose up --build -d```
launch composer : ```docker exec -it rendu_cms_php_php_1 bash```
after : ```composer dump```
Extract dotEnv.rar inside src/

website : ```localhost:5000/```